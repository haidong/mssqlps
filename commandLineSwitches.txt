1. Pipe everything out. User can use greater than symbol to save the output if s/he wants to;
2. -i indicates instance(s) a script is targeted at. If there is a -f parameter, that dictates that the input parameter is a file name, which lists all instances in a file, each instance is separated by a new line character. The absence of the -f parameter indicates that what follows -i are instance(s). If more than one instance, it should be delimited by a comma;
3. To the extent that is possible, follow the command line switches used in sqlcmd, such as -S for instance, -E for integrated security, etc.
