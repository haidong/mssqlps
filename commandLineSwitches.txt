1. Pipe everything out. User can use greater than symbol to save the output if s/he wants to;
2. Use -i for input parameter(s). If there is a -f parameter, that dictates that the input parameter is a file name, which lists all input parameters in a file, each input is separated by a new line character. The absence of the -f parameter indicates that input parameters;
3. To the extent that is possible, follow the command line switches used in sqlcmd, such as -S for instance, -E for integrated security, etc.
